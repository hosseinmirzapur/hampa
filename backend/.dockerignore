# Git
.git
.gitignore

# Node modules
node_modules

# Build artifacts (if not needed in intermediate stages)
dist
# bun.lockb # Usually included to ensure consistent installs

# Environment files (should be managed outside the image)
.env
.env.*
!/.env.example # Include example env file if it exists

# Docker files
Dockerfile
docker-compose.yml
.dockerignore

# IDE and OS specific files
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log* # If bun generates debug logs

# Test files (if not running tests inside Docker build)
test/
src/**/*.spec.ts
src/**/*.e2e-spec.ts
